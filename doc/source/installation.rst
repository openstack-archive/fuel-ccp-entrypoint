============
Installation
============

At the command line::

    $ pip install ms-ext-config

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv ms-ext-config
    $ pip install ms-ext-config
